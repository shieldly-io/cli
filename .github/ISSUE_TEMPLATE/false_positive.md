---
name: False positive
about: A rule fired on a policy that shouldn't have triggered it
title: "[false-positive] "
labels: false-positive
---

**Rule ID** (from the finding, e.g. `BR-DATA-S3`):

**Policy statement that triggered it** (redact resource ARNs/account IDs if needed):
```json

```

**Why you believe this is a false positive:**

**Workaround you're using today** (e.g. `--ignore <rule-id>`, if any):
